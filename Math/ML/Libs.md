```table-of-contents
```
---
# `NumPy`
Для поиска функций по описанию можно использовать `np.lookfor`.
Ещё можно использовать такое:
`?np.linspace`
При создании среза массив не создаётся. Его надо копировать.