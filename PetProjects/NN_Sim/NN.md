[[Start Page|На главную]]

---
С точки зрения отображения памяти общая система состоит из слоёв, каждый слой состоит из весовых коэффициентов, который соединяют его с предыдущим слоем.

Таким образом у нас формируется двумерный массив данных.

# Динамическая архитектура
Нужно создать функцию, которая должна будет решать, создавать ли новые блоки нейросетей, например с помощью механизма attention в Google Titans. Нужно убедится, что мы должны создавать новый блок памяти, а не прибегать к дообучению существующего. Также возможно использовать Z-вектора для оценки размеров новых блоков, а не только для указания, какую область нейросети задействовать. Или эти Z-вектора будут определять создание различных областей знаний внутри модели. То есть создание блока, отвечающего за матан, создание блока, отвечающего за абстрактную речь и т.д. Можно создать вспомогательную нейронку которая будет отвечать за подбор параметров для своей эволюции.

# Токенизация по буквам
Произведём one-hot кодирование каждой буквы. Объединим буквы в слова. Тогда мы получим матрицу, где количество строк соответствует мощности алфавита, а количество строк - количеству букв (токенов) в слове. Размер входящих слов может быть фиксированным, тогда нам крайне важно находить зависимости в последовательности данных слов. Можно попробовать увеличить количество входных матриц.
# Старение нейронов
При ReLU активации нейрона посмотреть, что будет, если со временем работы модели или в зависимости от частоты срабатывания или в зависимости от количества срабатывания нейрона с момента инициализации модели будет изменяться смещение (bias), а частности повышаться до 1,5 как в человеческом мозге (рис. 3.6 стр. 111 Книга Глубокое обучение нейросетей). Как это отразиться на самообучении и блокировке обучения "с возрастом сети". Можно учесть вариации ReLU (рис. 3.7)

# Разделение направления сигнала (forward and backward)
Сигнальные петли в реккуренных сетях: каждый слой должен обладать параметром, который определяет, будет ли сигнал идти дальше или возвращаться на предыдущий слой. Или он будет расщипляться и в каких пропорциях это будет происходить. TODO: 


